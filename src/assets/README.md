# Assets

Directory reserved for local image and font assets. The original pixel garden and avatar are currently reusable SVG components in `src/components/`, so no external images or font downloads are required.
