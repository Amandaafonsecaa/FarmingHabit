import type { User } from '../types/dashboard';
import { Icon } from './Icon';
export function WellbeingCard({ user }: { user: User }) {
 return <section className="card p-6" aria-labelledby="wellbeing-heading"><h2 id="wellbeing-heading" className="section-title">Como você está?</h2><p className="mt-1 text-xs text-muted">Cuidar de você também é progredir.</p><div className="mt-5 flex items-center gap-3"><span className="flex size-10 items-center justify-center rounded-full bg-[#f7edce] text-xl text-[#aa884a]" aria-hidden="true">☺</span><div><p className="text-[10px] font-semibold uppercase tracking-wider text-muted">Humor de hoje</p><p className="mt-1 text-sm font-medium">{user.mood}</p></div></div><div className="mt-5 border-t border-line pt-4"><div className="mb-2 flex items-center justify-between text-xs"><span className="flex items-center gap-2 text-muted"><Icon name="sun" className="size-4"/> Sua energia</span><span className="font-semibold text-olive">{user.energy} de 5 · Boa</span></div><div className="flex gap-1.5" role="meter" aria-label="Energia" aria-valuenow={user.energy} aria-valuemin={0} aria-valuemax={5}>{Array.from({ length: 5 }, (_, index) => <div key={index} className={`h-2 flex-1 rounded-sm ${index < user.energy ? 'bg-[#a5b585]' : 'bg-[#eeeee5]'}`}/>)}</div></div></section>;
}
