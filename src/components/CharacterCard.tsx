import type { User } from '../types/dashboard';
import { PixelAvatar } from './PixelAvatar';
import { Icon } from './Icon';
import { ProgressBar } from './ProgressBar';
export function CharacterCard({ user }: { user: User }) {
 return <section className="card p-6" aria-labelledby="character-heading"><div className="flex items-center justify-between"><h2 id="character-heading" className="section-title">Seu cantinho</h2><Icon name="spark" className="size-4 text-gold"/></div><div className="relative my-5 flex h-36 items-end justify-center overflow-hidden rounded-xl border border-[#e3e8d8] bg-[#f0f3e8]"><div className="absolute inset-x-0 bottom-0 h-9 bg-[#e0e8cd]"/><span className="absolute left-8 top-6 text-xl text-gold" aria-hidden="true">✧</span><span className="absolute right-9 bottom-6 text-2xl text-sage" aria-hidden="true">✿</span><PixelAvatar className="relative mb-2 h-31 w-28"/></div><div className="flex items-center justify-between"><div><p className="font-display text-xl font-semibold">{user.name}</p><p className="mt-1 text-xs text-muted">Cultivando novos começos</p></div><span className="rounded-lg border border-[#e0e5d5] bg-[#f1f4e9] px-3 py-2 text-xs font-bold text-olive">Nível {user.level}</span></div><div className="mb-2 mt-5 flex justify-between text-[11px]"><span className="font-semibold text-olive">Seu próximo florescer</span><span className="text-muted">{user.xp} / {user.xpPerLevel} XP</span></div><ProgressBar value={user.xp / user.xpPerLevel * 100} label="Experiência para o próximo nível"/><p className="mt-2 text-right text-[10px] text-muted">Faltam {user.xpPerLevel - user.xp} XP para o nível {user.level + 1}</p></section>;
}
