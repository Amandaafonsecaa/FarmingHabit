# Types

`dashboard.ts` describes only the existing frontend mock data, not a database schema.

Once the product requirements and Supabase tables are defined, place generated database types in `database.ts` and supply the generated `Database` type to `createClient`. No database model or placeholder tables are defined yet.
